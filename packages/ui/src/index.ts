/**
 * @zona/ui — интерфейс наблюдателя (React + zustand): схематичная карта,
 * радио-лог, летопись, инспектор сущностей, управление временем.
 * Только читает снапшоты/события ядра, никогда не мутирует симуляцию напрямую.
 * Наполняется начиная с UI-фаз.
 */
export {};
