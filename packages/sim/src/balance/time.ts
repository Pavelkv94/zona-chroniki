/**
 * @module @zona/sim/balance/time
 *
 * Балансовые константы времени симуляции (закон №7: никаких «магических чисел»
 * длительностей в коде систем/CLI — только отсюда).
 *
 * Модель времени: 1 тик планировщика = 1 игровая минута. Отсюда длина суток —
 * число тиков в сутках. Эта константа нужна, например, headless-CLI, чтобы
 * перевести `--days N` в число тиков (`N * TICKS_PER_DAY`), не хардкодя 1440.
 *
 * Пример:
 * ```ts
 * import { TICKS_PER_DAY } from '@zona/sim';
 * const ticks = days * TICKS_PER_DAY;
 * ```
 */

/** Тиков в одних игровых сутках (1 тик = 1 минута → 24 * 60 = 1440). */
export const TICKS_PER_DAY = 1440;
