/**
 * @module @zona/sim/balance/disease
 *
 * Балансовые константы БОЛЕЗНЕЙ (Фаза 5, задача 5.1, закон №7: ставки/пороги живут
 * ТОЛЬКО здесь, не магическими числами в системе Sickness 5.8). Свойства самих
 * болезней-контента (заразность/летальность/инкубация) — в data/diseases.json
 * (закон №10); здесь — МЕХАНИКА заражения/течения/лечения, общая для всех болезней.
 *
 * ── Причинность вместо шанса (закон №2) ──────────────────────────────────────
 * Болезнь возникает из СОСТОЯНИЯ мира, не из «X% заболеть»: экспозиция
 * (`Sickness.exposure`, шкала 0..1) КОПИТСЯ причинно — от контакта с больным соседом
 * и/или от холодной погоды (для coldBorne-болезней) — и при пересечении
 * INFECTION_THRESHOLD носитель заболевает (`disease>0`, severity начинает расти). Без
 * лечения тяжесть растёт и наносит урон здоровью (реальная мортальность, закон №1);
 * TREAT (TaskKind=12) сбивает тяжесть. Всё детерминировано (закон №8). Значения
 * СТАРТОВЫЕ; тюнит balance-analyst по метрикам длинного headless-прогона.
 */

/**
 * Прирост экспозиции ЗА КОНТАКТ с больным носителем (шкала 0..1). Домножается на
 * `DiseaseData.transmissibility` конкретной болезни (заразная передаётся сильнее).
 * Экспозиция копится причинно (закон №2) — заражение из СОСТОЯНИЯ (рядом больной), а
 * не «X% шанс подхватить».
 */
export const EXPOSURE_PER_SICK_CONTACT = 0.05;

/**
 * Прирост экспозиции ЗА ТИК в ХОЛОДНУЮ погоду (для coldBorne-болезней, напр. простуда).
 * Источник из СРЕДЫ (закон №2): холод сам по себе накапливает риск, без больного рядом.
 * Домножается на длительность шага системы болезней (компенсация редкого шага).
 */
export const EXPOSURE_COLD_WEATHER = 0.01;

/**
 * Порог экспозиции, при пересечении которого носитель ЗАБОЛЕВАЕТ (disease>0, severity
 * стартует). 1.0 = экспозиция «накопилась полностью» (шкала 0..1). Порог из состояния
 * мира (закон №2), не бросок кости.
 */
export const INFECTION_THRESHOLD = 1.0;

/**
 * Базовая скорость роста тяжести (`Sickness.severity`, 0..1) ЗА ТИК болезни без
 * лечения. Домножается на `DiseaseData.severityRate` конкретной болезни. Прогресс
 * детерминирован (закон №2), домножается на длительность шага системы.
 */
export const SEVERITY_RISE_PER_TICK = 0.001;

/**
 * Множитель урона здоровью ОТ ТЯЖЕСТИ болезни за тик: урон = `severity *
 * DiseaseData.lethality * SEVERITY_DAMAGE_MULT`. Тяжёлая инфекция убивает быстрее
 * лёгкой простуды (реальная мортальность, закон №1), а не «X% смерть».
 */
export const SEVERITY_DAMAGE_MULT = 4;

/**
 * Тиков выздоровления по умолчанию, если у болезни-контента срок не переопределён:
 * при экспозиции ниже порога тяжесть спадает и через этот срок носитель здоров. Целое
 * >0 (детерминированный срок, закон №8). Конкретная болезнь несёт свой `recoveryTicks`.
 */
export const RECOVERY_TICKS = 4320;

/**
 * Тяжесть (`Sickness.severity`), при достижении которой болезнь ЛЕТАЛЬНА (носитель
 * гибнет от истощения болезнью). 1.0 = «полная» тяжесть (шкала 0..1). Смерть из
 * состояния (закон №1/№2), не «X% умереть».
 */
export const SICKNESS_LETHAL_SEVERITY = 1.0;

/**
 * На сколько TREAT (TaskKind=12) сбивает тяжесть болезни за одно применение (шкала
 * 0..1). Лечение ПРИЧИННО снижает тяжесть; медикамент (kind:'medical') физически
 * расходуется (закон №3, система 5.8).
 */
export const TREAT_SEVERITY_DROP = 0.5;
