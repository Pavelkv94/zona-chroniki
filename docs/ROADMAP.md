# Дорожная карта — «Зона: Хроники»

Статусы: ⬜ не начато · 🟡 в работе · ✅ принято (прошло QA + ревью) · ⏸ заблокировано

## Шаг 0 — Подготовка скелета ✅
Монорепо npm workspaces, ESM, strict TS, 4 пакета `@zona/{shared,sim,headless,ui}`,
закон №5 enforced компилятором. Решения: D-000..D-002.

## Фаза 0 — Ядро симуляции (headless) ✅ ЗАКРЫТА
Спроектирована `sim-architect`. Контракты и риски: DECISIONS.md D-003..D-017.

Граф зависимостей:
```
0.1 ─┬─> 0.2 ──> 0.5 ─┬─> 0.6 ─┐
     ├─> 0.3 ─────────┤        ├─> 0.7 (гейт, закрывает фазу)
     └─> 0.4 ──> 0.5 ─┘        │
0.2 ──────────────> 0.6 ───────┘
```

| Задача | Что | Зависит от | Статус |
|--------|-----|-----------|--------|
| 0.1 | Каркас ядра: типы @zona/shared, `SimWorld`, `ResourceStore`, обёртка bitecs | — | ✅ |
| 0.2 | Планировщик тиков с частотами `(t-phase)%every===0`, `bus.endTick` | 0.1 | ✅ |
| 0.3 | Seeded PRNG mulberry32 + `fork(label)` через `hash(label)^seed` | 0.1 | ✅ |
| 0.4 | Шина событий: append-only лог, монотонный `EventId`, `causedBy` | 0.1 | ✅ |
| 0.5a | Канонизатор + `hashSnapshot` (FNV-1a) + `serialize` + `exportEntityIndex` (ecs-seam) | 0.1–0.4 | ✅ |
| 0.5b | `deserialize` + `createEcsWorldFromIndex` + гейт round-trip/resume-детерминизма | 0.5a | ✅ |

### Открытые хвосты, которые ОБЯЗАНА закрыть задача 0.5 (сериализация):
- **SoA-компоненты при reuse eid (D-008):** `purgeEntity` чистит только ResourceStore, не SoA bitecs. Сериализовать/восстанавливать только ЖИВЫЕ сущности; не захватывать компоненты освобождённых eid.
- **Восстановление форков PRNG (0.3):** `Rng` отдаёт только `state`; seed подпотока выведен из (label, rootSeed). Снапшот обязан хранить label или seed каждого потока, иначе восстановленный fork не воспроизведёт детей (D-004).
- **Монотонность EventId через save/load (C-4):** seam уже готов — `createEventBus(getTick, { eventSeq, log })` принимает стартовый счётчик и лог. 0.5 обязана их сохранять/восстанавливать.

## Будущие задачи (после закрытия Фазы 0)
| # | Что | Причина | Приоритет |
|---|-----|---------|-----------|
| P-1 | Индекс по тику для `EventBus.at()/drainSince()` (сейчас O(n) filter по всему логу; лог отсортирован по тику → бинарный поиск границ или `Map<Tick,[start,end]>`) | На горизонте 100 дней — миллионы событий, реактивные системы зовут `at(t-1)` каждый тик → O(n·систем·тиков) | Перф, до нагрузочных прогонов balance-analyst |
| 0.6 | Headless CLI `--days --seed --metrics` (тайминг только здесь) | 0.2, 0.5 | ✅ |
| 0.7 | Гейт детерминизма: интеграционный тест 2×1000 тиков, mermaid-диаграммы | 0.5, 0.6 | ✅ |

**Фаза 0 ЗАКРЫТА (2026-07-03).** DoD выполнен: `npm run test` — 302 зелёных, `npm run typecheck` — 0 ошибок,
`npm run sim:100days` — без ошибок (hash=c5dff40f). Детерминизм ядра доказан интеграционно (D-017).
Каждая задача прошла core-engineer → qa-engineer → code-reviewer. Готово к Фазе 1.

Процесс на каждую задачу: core-engineer → qa-engineer → code-reviewer → интеграция.

## Фаза 1 — Живые тела (20 сталкеров + животные на графе 10 локаций)
Спроектирована `sim-architect`. Контракты/риски: DECISIONS.md D-018..D-023.
DoD фазы: прогон 10 дней — 0 idle, NPC выживают охотой, смерти объяснимы (полная цепочка causedBy).

Порядок: {1.0, 1.1} → 1.2 → {1.4,1.5,1.6,1.7} → 1.3 → 1.8 → {1.9,1.10} → 1.11 → 1.12 → 1.13.

| # | Задача | Зависит | Исполнитель | Статус |
|---|--------|---------|-------------|--------|
| 1.0 | Реестр компонентов + сериализация SoA (снять fail-fast D-016, resume round-trip) | — | core | ✅ |
| 1.1 | data JSON (map/items/species/names) + загрузчик + balance-константы | — | core (+ecosystem) | ✅ |
| 1.2 | Определения компонентов (Position/Needs/Health/Task/Skills/Home/Animal/WorldClock+теги) | 1.0 | core | ⬜ |
| 1.3 | worldgen: 20 сталкеров (имена, инвентарь-источник), стада, расстановка от seed | 1.1,1.2 | behavior | ⬜ |
| 1.4 | Movement (переход по графу, etaTicks, departed/arrived) | 1.1,1.2 | core | ⬜ |
| 1.5 | Needs (голод/жажда/усталость/страх, пороги, урон истощения) | 1.2 | behavior | ⬜ |
| 1.6 | Weather/DayNight (погода в WorldClock, isNight из tick) | 1.2 | core | ⬜ |
| 1.7 | Perception (бакеты по loc + смежные, contacts, spotted) | 1.1,1.2 | core | ⬜ |
| 1.8 | TaskSelection (utility argmax, idle запрещён, task/selected) | 1.4,1.5,1.7 | behavior | ⬜ |
| 1.9 | Animals (стада, пастьба/бегство, причинное размножение) | 1.2,1.7 | ecosystem | ⬜ |
| 1.10 | Encounter Resolver v1 + Encounters (охота человек-vs-животное, единый интерфейс) | 1.7,1.8 | behavior | ⬜ |
| 1.11 | Death/Corpse (труп с инвентарём, entity/died с causedBy) | 1.10,1.5 | core | ⬜ |
| 1.12 | CLI `--log verbose` (человекочитаемый рендер лога) | 1.10 | core (headless) | ⬜ |
| 1.13 | QA-гейт Фазы 1 (10 дней: 0 idle, выживание охотой, цепочки смертей, детерминизм) | все | qa | ⬜ |
