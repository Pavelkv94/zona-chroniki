# Журнал решений — «Зона: Хроники»

Формат записи:

```
## D-XXX | <Фаза> | <YYYY-MM-DD>
Решение: ...
Причина: ...
Альтернативы: ... (что отклонили и почему)
Затрагивает: <системы/файлы>
```

---

## D-000 | Шаг 0 | 2026-07-03
Решение: Скелет монорепо на npm workspaces (`packages/*`), ESM (`"type":"module"`),
TypeScript strict, `moduleResolution: "Bundler"`, запуск через `tsx`/`vitest`
без отдельного build-шага. Пакеты: `@zona/shared`, `@zona/sim`, `@zona/headless`,
`@zona/ui`. Внутренние импорты — по алиасам `@zona/*` (symlink workspaces + `paths`).
Причина: ядру нужен headless-запуск в Node без бандлинга; tsx/vitest исполняют TS
напрямую, поэтому build не нужен до появления UI-сборки.
Альтернативы: pnpm workspaces (отклонено — у пользователя npm); ts-node
(отклонено в пользу tsx — быстрее, ESM из коробки); project references с composite
(отклонено — требует declaration/outDir, лишнее без build-шага).
Затрагивает: package.json, tsconfig.base.json, все packages/*/tsconfig.json.

## D-001 | Шаг 0 | 2026-07-03
Решение: Закон №5 (пакет `sim` не тянет DOM/React/Node) сделан ошибкой компиляции:
в `packages/sim/tsconfig.json` намеренно нет `lib: "DOM"` и `types: []` (без node).
Аналогично `shared`. `ui` получает `lib: DOM` + `jsx: react-jsx`, `headless` —
`types: ["node"]` (единственный пакет с доступом к process/fs).
Причина: превратить конституционный закон из декларации в проверяемый инвариант —
любой запретный импорт падает на `npm run typecheck`.
Альтернативы: eslint-правило no-restricted-imports (отклонено — компилятор строже
и не обходится).
Затрагивает: packages/sim/tsconfig.json, packages/ui/tsconfig.json.

## D-002 | Шаг 0 | 2026-07-03
Решение: `npm run test` = `vitest run --passWithNoTests` (exit 0 при нуле тестов).
Причина: чтобы CI-smoke и локальный прогон были зелёными до появления первых тестов
Фазы 0, а не ложно-красными.
Альтернативы: держать exit 1 (отклонено — ломает будущий CI-gate на пустом наборе).
Затрагивает: package.json → scripts.test.

## D-003 | Фаза 0 | 2026-07-03
Решение: `EntityId`/`EventId`/`LocationId` — branded `number` в `@zona/shared`, без импорта bitecs.
В `@zona/sim` они совместимы с `EntityId` bitecs (тоже number).
Причина: shared/ui/headless видят контракты, не таща ECS-движок; сохраняет закон №5 и слабую связанность.
Альтернативы: реэкспорт `EntityId` из bitecs в shared (отклонено — тянет движок в чистый пакет типов).
Затрагивает: `@zona/shared/src`, все контракты ядра.

## D-004 | Фаза 0 | 2026-07-03
Решение: `Rng.fork(label)` выводит seed ребёнка из `hash(label) ^ rootSeed`, а не из текущего state родителя.
Причина: инвариант детерминизма — добавление вызовов в одной системе не должно ломать последовательности
других (риск C-2).
Альтернативы: fork от текущего state (отклонено — хрупко); один глобальный поток на весь мир
(отклонено — любая перестановка систем ломает историю).
Затрагивает: `core/rng.ts`, все системы (берут `fork(system.name)`).

## D-005 | Фаза 0 | 2026-07-03
Решение: шина — append-only лог с монотонным `EventId`; события тика фиксируются в `endTick`;
реактивные системы читают `at(tick-1)`. Счётчик id (`eventSeq`) сериализуется.
Причина: развязать системы от внутритикового порядка (C-1), сохранить причинные цепочки через
save/load (C-4), закон №6.
Альтернативы: немедленная доставка подписчикам в тот же тик (отклонено — зависимость от порядка систем);
эфемерная очередь без лога (отклонено — ломает летопись/причинность).
Затрагивает: `core/events.ts`, `core/scheduler.ts`, `core/snapshot.ts`.

## D-006 | Фаза 0 | 2026-07-03
Решение: любой замер времени и бюджет 1.6 мс/тик реализуются только в `@zona/headless`;
`@zona/sim` не измеряет время и не принимает решений на его основе.
Причина: разрешить конфликт GDD п.13.4 (бюджет) с законом №8 (запрет Date.now/недетерминизма).
Время не должно влиять на состояние мира.
Альтернативы: адаптивный пропуск систем по времени (отклонено — делает историю зависимой от железа).
Затрагивает: `@zona/headless/cli.ts`, бенчи; `core/scheduler.ts` (без таймеров).

## D-007 | Фаза 0 | 2026-07-03
Решение: не-числовые данные NPC (имена, инвентарь, память, отношения) хранятся в `ResourceStore`,
а не в SoA bitecs; итерация и сериализация — только по отсортированным ключам.
Причина: SoA bitecs хранит числа; закон №8 требует сортировки ключей; отделяет «горячие» числовые
компоненты от «холодных» объектных данных.
Альтернативы: сериализация сырых SoA-массивов целиком (отклонено — захват мусора удалённых eid, C-6).
Затрагивает: `core/world.ts`, `core/snapshot.ts`.

## D-008 | Фаза 0 / задача 0.1 | 2026-07-03
Решение: Проблема «призрака» при переиспользовании eid (риск C-6) закрывается очисткой
ресурсов, а НЕ версионированием eid в bitecs. Введена единственная публичная точка удаления
`destroyEntity(world, eid)` в core/world.ts = низкоуровневый `destroyEcsEntity` (ecs.ts,
внутренний) + `resources.purgeEntity(eid)`. `withVersioning` bitecs не включаем.
Причина: версионирование меняет кодировку eid (id+version в одном числе) и усложнило бы
сериализацию 0.5; purge полностью устраняет наследование «холодных» данных покойника
(имя/инвентарь → нарушение закона №3). Подтверждено QA (инвертированный F-1 тест) и ревью.
Альтернативы: withVersioning (отклонено — сложность кодировки eid); чистить в каждой системе
вручную (отклонено — забудут, единая точка надёжнее).
Затрагивает: core/ecs.ts (destroyEcsEntity внутренний), core/world.ts (destroyEntity, purgeEntity),
core/index.ts (не реэкспортирует низкоуровневый ecs-слой).
ОТКРЫТЫЙ ХВОСТ для 0.5: purge чистит только ResourceStore, но НЕ SoA-компоненты bitecs при
reuse eid. Сериализация 0.5 обязана сохранять/восстанавливать только ЖИВЫЕ сущности и не
захватывать компоненты освобождённых eid.

## D-009 | Фаза 0 / задача 0.2 | 2026-07-03
Решение: RNG, который планировщик кладёт в `SystemCtx.rng`, — это `world.rng.fork(`${system.name}@${tick}`)`,
т.е. метка форка включает НОМЕР ТИКА, а не только имя системы.
Причина: уточнение/исправление к D-004. `fork(label)` детерминирован от `(label, rootSeed)` и НЕ
зависит от состояния родителя — поэтому `fork(system.name)`, вычисляемый заново каждый тик, давал бы
ОДНУ И ТУ ЖЕ последовательность на каждом тике (первый разброс выстрела был бы идентичен из тика в тик).
Включение тика в метку даёт разный поток по тикам при полном детерминизме. Бонус: per-system RNG
становится БЕЗ СОСТОЯНИЯ — поток однозначно определяется `(rootSeed, system.name, tick)`, поэтому 0.5
НЕ обязана сохранять rng-состояние на каждую систему (в отличие от «холодных» форков, что живут дольше тика).
Альтернативы: персистентный форк на систему, живущий между тиками (отклонено — вводит per-system
rng-состояние в снапшот, лишняя поверхность сериализации и рассинхрона); форк меткой только по имени
(отклонено — повтор последовательности каждый тик, скрытый недетерминизм физиологии).
Затрагивает: core/scheduler.ts (сборка SystemCtx), core/system.ts (SystemCtx = { world, bus, rng, tick }),
все будущие системы, использующие ctx.rng.

## D-010 | Фаза 0 / задача 0.2 | 2026-07-03
Решение: (1) Тик АТОМАРЕН. Если система бросает в update, планировщик вызывает `bus.discardTick()`
(сброс внутритикового буфера без коммита) и пробрасывает исключение, НЕ вызывая endTick и НЕ сдвигая
world.tick. Мир остаётся в состоянии «до тика». (2) `register` требует непустое УНИКАЛЬНОЕ имя системы
(держит Set<SystemName>, бросает на дубль/пустое).
Причина: (1) без атомарности упавший+повторённый тик коммитил бы события ДВАЖДЫ в append-only лог —
нарушение законов №8/№3 (событие «из воздуха»). (2) метка форка rng по D-009 = `${name}@${tick}`, поэтому
дубль имени дал бы двум системам ИДЕНТИЧНЫЙ поток случайности (коллизия физиологии) + двойное исполнение.
Побочно: id событий отброшенного тика «сгорают» — пропуски в EventId допустимы (контракт требует
монотонности/уникальности, не непрерывности; восстановление после падения — отдельное исполнение, а не
эталонный детерминированный прогон).
Альтернативы: откат eventSeq при discard (отклонено — лишняя связанность, пропуски безвредны); отложить
атомарность как долг (отклонено ревью-советом — двойной коммит в неизменяемый лог слишком опасен для ядра).
Затрагивает: core/scheduler.ts (try/catch/rethrow, Set имён), core/events.ts (метод discardTick, аддитивно).

## D-011 | Фаза 0 / задача 0.5 | 2026-07-03
Решение: eid при deserialize восстанавливаются ТОЧНО через сериализацию/восстановление аллокатора bitecs
`EntityIndex` verbatim (плоские `dense[]/sparse[]/aliveCount/maxId`, versioning=false по D-008). `ecs.ts`
получает две аддитивные внутренние функции: `exportEntityIndex(ecsWorld)` и `createEcsWorldFromIndex(snap)`
(= `createWorld(restoredIndex)`); `snapshot.ts` работает с индексом как с непрозрачным blob через обёртку
`ecs.ts`, не касаясь bitecs напрямую (слой D-008).
Причина: сохраняет тождество eid И порядок freelist → resume совпадает с непрерывным прогоном побитово
(GDD 5.3). eid'ы, зашитые в ключах ResourceStore и в payload'ах событий append-only лога, остаются
валидными БЕЗ ремапа — core не знает схему доменных payload'ов (закон №6) и не может их переписать.
Закрывает хвост D-008: восстанавливаются только живые eid + аллокатор; SoA-слоты покойников не перечисляются.
Альтернативы: remap old→new (отклонено — не переписать eid внутри событий летописи, C-4/закон №6);
собственный аллокатор eid (отклонено — bitecs уже отдаёт аллокатор как данные); addEntity по порядку с
долгом «дыры» (отклонено — при гибели NPC дыры дают другой порядок freelist на resume → скрытый P0).
Затрагивает: core/ecs.ts (аддитивно), core/snapshot.ts, привязка к bitecs ^0.4.0 (миграция ловится
round-trip-тестом при мажоре).

## D-012 | Фаза 0 / задача 0.5 | 2026-07-03
Решение: `SnapshotJSON = { version:1, seed, tick, rngState, eventSeq, ecsIndex, entities[], resources{},
components{}, eventLog[] }`. `hashSnapshot` — FNV-1a по каноничной строке рекурсивного канонизатора (ключи
объектов сортируются по возрастанию; массивы сохраняют порядок; числа через `Number.toString`;
`NaN/±Infinity/undefined/функции` → throw). `ecsIndex` — авторитет по живым eid; `entities[]` (сорт.) —
производное, сверяется на deserialize. `components:{}` в Фазе 0; форма на будущее —
`{ name: { fields:{f:number[]}, eids:EntityId[] } }` только по живым eid.
Причина: `JSON.stringify` даёт порядок ключей = insertion order → нестабильный хэш (нарушение закона №8).
Альтернативы: хэш поверх `JSON.stringify` (отклонено); `entities[]` как авторитет вместо ecsIndex
(отклонено — ecsIndex кодирует порядок freelist, нужный для resume).
Затрагивает: core/snapshot.ts, @zona/shared (тип SnapshotJSON).

## D-013 | Фаза 0 / задача 0.5 | 2026-07-03
Решение: значения ResourceStore — только plain-JSON-данные; `Map/Set/классы/функции/undefined` запрещены
(Map-подобное хранится как отсортированный массив `[eid,value]`). Сериализация ресурсов — рекурсивная
канонизация (сорт ключей), НЕ голый `JSON.stringify`; канонизатор бросает на запретных значениях с
указанием `(key, eid)`.
Причина: порядок ключей объекта нестабилен между прогонами → ломает хэш (закон №8, C-3); Map/Set-итерация
без сортировки прямо запрещена законом №8. Ранний throw ловит нарушение в тесте/ревью.
Альтернативы: `JSON.stringify` без канонизации (отклонено); per-key кодеки (отклонено для Фазы 0).
Затрагивает: core/snapshot.ts (канонизатор), контракт ResourceStore (значения = plain data), профильные
инженеры (relations/memory как сорт. массивы).

## D-014 | Фаза 0 / задача 0.5 | 2026-07-03
Решение: снапшот сохраняет состояние ТОЛЬКО корневого rng (`rngState` uint32); per-system форки не
сохраняются (stateless, D-009); шина восстанавливается через seam `createEventBus(getTick,{eventSeq,log})`.
Полный набор round-trip: `{ ecsIndex, resources, tick, seed, rngState, eventSeq, eventLog }`. Граничный
инвариант: любой «холодный» форк, живущий дольше одного тика, обязан быть либо пересоздаваемым каждый тик
(метка включает tick, как D-009), либо его `state` добавляется в снапшот. В Фазе 0 таких форков нет.
Причина: минимизировать поверхность сериализации rng; зафиксировать условие достаточности `rngState` корня.
Альтернативы: сохранять per-system rng-состояние (отклонено — D-009); не фиксировать граничное правило
(отклонено — риск скрытого недетерминизма при первом долгоживущем форке).
Затрагивает: core/snapshot.ts, core/rng.ts (restoreRng), core/events.ts (init-seam), будущие системы.

## D-015 | Фаза 0 / задача 0.5 | 2026-07-03
Решение: 0.5 режется на 0.5a (канонизатор + `hashSnapshot` + `serialize` + `exportEntityIndex`) → 0.5b
(`deserialize` + `createEcsWorldFromIndex` + гейт round-trip/resume-детерминизма). Строго последовательно;
форма `SnapshotJSON` замораживается в 0.5a.
Причина: объём не влезает в одну сессию; 0.5a даёт независимо тестируемый артефакт (стабильный хэш).
Альтернативы: одна задача 0.5 (отклонено — превышает сессию).
Затрагивает: docs/ROADMAP.md, процесс core-engineer.

## D-016 | Фаза 0 / задача 0.5b | 2026-07-03
Решение: `createEcsWorldFromIndex` восстанавливает мир в ДВА шага, а не одним `createWorld(index)`.
Шаг 1: `createWorld(revivedIndex)` — bitecs распознаёт объект с `dense/sparse/aliveCount` и кладёт его
как `ctx.entityIndex` verbatim (аллокатор eid+freelist восстановлен; `entityExists`/`spawnEntity` работают).
Шаг 2: ВРУЧНУЮ населяем `ctx.entityComponents` (Map) пустым `Set` на каждый ЖИВОЙ eid (= первые
`aliveCount` элементов `dense`), т.к. `getAllEntities` bitecs 0.4 читает ИМЕННО `entityComponents`, а
`createWorld` оставляет её пустой → без шага 2 `allEntities(restored)` вернул бы `[]` при «живых» eid в
индексе. Заполняем прямой записью в Map, а НЕ через `addEntity`: `addEntity` выдал бы НОВЫЙ eid (recycle
из freelist или `++maxId`) и сдвинул аллокатор, сломав тождество eid и порядок freelist. Касание
`$internal` — только в `ecs.ts` (слой D-008). Плюс: `deserialize` держит GUARD `allEntities(ecs) ===
snap.entities` (авторитет — `ecsIndex`, D-012); при расхождении бросает (битый/подделанный снапшот).
Побочно: `world.ts` получил внутренний seam `createResourceStore()` (регидратация ресурсов в `deserialize`),
`snapshot.ts` — `deserialize` собирает `SimWorld` с той же инициализацией `bus ↔ world.tick`, что
`createSimWorld`, и DEEP-CLONE значений ресурсов (изоляция снапшота от мира). Ни `createEcsWorldFromIndex`,
ни `createResourceStore` не реэкспортируются из `@zona/sim`.
Причина: `getAllEntities` и `entityExists` bitecs 0.4 читают РАЗНЫЕ структуры (`entityComponents` Map против
`entityIndex`); восстановление одного индекса недостаточно для полноценного мира. Прямое население без
`addEntity` — единственный способ сохранить eid+freelist побитово (условие resume, D-011).
Альтернативы: `addEntity` в цикле по живым (отклонено — выдаёт новые eid, ломает freelist/тождество);
патчить bitecs, чтобы `getAllEntities` читал индекс (отклонено — форк движка, риск при апгрейде);
хранить список живых отдельно в снапшоте (отклонено — `ecsIndex` уже авторитет, дублирование рассинхронит).
Проверено гейтом resume-детерминизма (snapshot.deserialize.test.ts): split-прогон через save/load совпадает
с непрерывным по хэшу и логу событий.
Затрагивает: core/ecs.ts (createEcsWorldFromIndex — аддитивно), core/snapshot.ts (deserialize),
core/world.ts (createResourceStore — внутренний seam), core/index.ts (реэкспорт deserialize), привязка
к bitecs ^0.4.0 (структура entityComponents ловится resume-тестом при мажоре).

Правки по ревью 0.5b (та же задача, ужесточение guard'ов read-path):
- GUARD ресурсов (закон №3, симметрия entities-GUARD): `deserialize` строит множество живых eid и при
  регидратации бросает, если пара `[eid,value]` ссылается на НЕ живой eid. Иначе подделанный/битый снапшот
  прорастил бы «призрак», а следующий `spawnEntity` (reuse eid) унаследовал бы его — предмет/имя из воздуха.
- Согласованность аллокатора: `reviveEntityIndex` проверяет не только форму полей, но и инварианты —
  `maxId === dense.length` и обратимость `sparse[dense[i]] === i` для живых `i ∈ [0, aliveCount)`. Type-валидный,
  но рассогласованный blob (кривой sparse / расходящийся maxId) даёт неверный `entityExists`/`spawn`, поэтому
  падаем явно.
- FAIL-FAST по `components`: Фаза 0 их не хранит (`serialize` пишет `{}`); их восстановление НЕ реализовано.
  Непустой `snap.components` → throw, чтобы будущая фаза с реальными SoA-компонентами реализовала загрузку,
  а не потеряла состояние молча (восстановив пустые Set). ОБЯЗАТЕЛЬНЫЙ ХВОСТ для следующей фазы.

## D-017 | Фаза 0 — ЗАКРЫТИЕ | 2026-07-03
Решение: Фаза 0 (ядро симуляции, headless) закрыта. Задачи 0.1–0.7 приняты, каждая прошла цикл
core-engineer → qa-engineer → code-reviewer. DoD выполнен: `npm run test` — 302 зелёных, `npm run typecheck` —
0 ошибок, `npm run sim:100days` — без ошибок (hash=c5dff40f, детерминизм не деградирует).
Что сделано: ECS-мир на bitecs + ResourceStore (0.1), планировщик тиков с частотами и атомарным тиком (0.2),
seeded PRNG mulberry32 с stateless per-system форками (0.3), append-only шина событий с causedBy (0.4),
детерминированная сериализация + resume (0.5a/0.5b), headless CLI (0.6), интеграционный гейт детерминизма (0.7).
Детерминизм доказан не тривиально: 2×1000 тиков одним seed → идентичные лог+хэш при непустой «живой» истории
(рождения/смерти/reuse eid/цепочки causedBy), seed=43 отличается, resume на наполненной середине совпадает.
Тег `v0.0-phase0` НЕ ставился — работа ведётся без git по решению пользователя.
Открытые хвосты, перенесённые в будущее: P-1 (индекс по тику для EventBus.at/drainSince, ROADMAP), восстановление
SoA-компонентов в deserialize (D-016, fail-fast стоит), граничный инвариант долгоживущих rng-форков (D-014).
Затрагивает: весь packages/sim, packages/headless/cli.ts; готовность к Фазе 1.

## D-018 | Фаза 1 / задача 1.0 | 2026-07-03
Решение: Сериализация SoA-компонентов через ЦЕНТРАЛЬНЫЙ реестр (sim/core/registry.ts), форма снапшота
components[name]={eids:[живые,сорт],fields:{f:number[]}} (D-012). Fail-fast D-016 снимается; неизвестное имя
компонента → throw; компонент на мёртвом eid → throw (симметрия GUARD ресурсов, закон №3). Resume-гейт
расширен round-trip'ом компонентов.
Причина: Фаза 1 впервые вводит реальные компоненты; без этого determinism-DoD рушится.
Альтернативы: сериализация сырых typed-массивов целиком (отклонено — захват мусора мёртвых eid, C-6);
хранить схему в bitecs (отклонено — bitecs не даёт стабильного порядка/имён).
Затрагивает: core/ecs.ts (add/has/remove/query), core/registry.ts, core/snapshot.ts, shared/snapshot.

## D-019 | Фаза 1 / задача 1.2 | 2026-07-03
Решение: Раскладка данных — числовое горячее в SoA (Position/Needs/Health/Task/Skills/Home/Animal/WorldClock),
объектное в ResourceStore (name/faction/profession/inventory/money) по D-007. День/ночь НЕ хранится (выводится
из tick % TICKS_PER_DAY), погода хранится (WorldClock: есть длительность+rng). Транзит без sentinel:
Position.dest===loc ⇒ стоит.
Причина: минимизировать состояние снапшота и поверхность рассинхрона; закон №8.
Альтернативы: хранить isNight (отклонено — дублирует детерминированную функцию tick).
Затрагивает: core/registry.ts, balance/time.ts, все системы.

## D-020 | Фаза 1 / задача 1.8 | 2026-07-03
Решение: idle запрещён через задачу-fallback FORAGE/REST (всегда score>0), а не пустую задачу (закон №4).
TaskSelection — детерминированный argmax; равные оценки разрешаются порядком enum, НЕ rng. rng системы
используется только на разброс выстрела в резолвере.
Причина: закон №2 (причинность, не X%) и закон №8 (детерминизм решений).
Альтернативы: rng-tie-break (отклонено — скрытый недетерминизм); допустить idle (отклонено — №4).
Затрагивает: systems/task-selection, balance/utility.ts.

## D-021 | Фаза 1 / задача 1.3 | 2026-07-03
Решение: Стартовый инвентарь/деньги 20 сталкеров ФИЗИЧЕСКИ приносятся извне при входе в Зону (GDD 4.7 приток):
ПМ+консервы+немного денег — источник «внесено из-за Периметра». Расстановка детерминирована seed
(rng.fork('worldgen')), НЕ возле игрока (игрока нет).
Причина: закон №3 (ничего из воздуха) требует физического источника даже для стартового набора.
Альтернативы: «дать на старте по факту» (отклонено — нарушение №3, нет источника).
Затрагивает: worldgen, data/names.json, data/items.json, balance/worldgen.ts.

## D-022 | Фаза 1 / задача 1.10 | 2026-07-03
Решение: Encounter Resolver — чистая функция над Combatant[][] (≥2 стороны), не хардкодит вид. Животное =
Combatant с ammo=0, melee>0, power из species.json. Человек-vs-человек (Фаза 2) — обе стороны из людей той же
сигнатурой. rng резолвера — только разброс попадания.
Причина: GDD 7 «резолвер един для всех»; избежать переписывания сигнатуры в Фазе 2.
Альтернативы: отдельная функция huntAnimal (отклонено — дублирование, ломает единый контракт GDD 7).
Затрагивает: systems/encounter (resolver + Encounters), shared/events.

## D-023 | Фаза 1 / задача 1.7 | 2026-07-03
Решение: Perception партиционирует по loc (+смежные рёбра для ranged-spot); результат — cold 'contacts' в
ResourceStore (сорт. массив eid). Внутритиковый обмен систем идёт через компоненты/contacts; причинные цепочки —
через бус с at(tick-1) (D-005). n² ограничен размером бакета.
Причина: бюджет 1.6 мс (D-006) + закон №6 (общение только через компоненты/шину).
Альтернативы: all-pairs по миру (отклонено — n² по N); мгновенная бус-доставка в тот же тик (отклонено — D-005).
Затрагивает: systems/perception, systems/encounter, systems/task-selection.

## D-024 | Фаза 1 / задача 1.0 (правка ревью) | 2026-07-03
Решение: «Призрак SoA-компонента» при reuse eid закрывается АКТИВНЫМ занулением полей в единой точке
`addComponent(world, comp, eid)`: перед тем как сущность станет носителем, все поля компонента для этого
eid зануляются (`store[field][eid] = 0`). bitecs 0.4 переиспользует освобождённые eid, а
`removeComponent`/`destroyEntity` НЕ трогают SoA-массивы — без зануления новый носитель, не переписавший
поле, читал бы «холодное» значение покойника. Это (а) течёт в снапшот (значение из воздуха, закон №3) и
(б) рассинхронит continuous-vs-resume МЕЖДУ процессами (закон №8: сохранивший процесс держит стухшее X в
глобальном массиве, свежий после load стартует с нуля). Инвариант: любой носитель начинает с известного
чистого состояния независимо от прошлого владельца eid; дальше поле пишет система. Не-носители в снапшот
не попадают, поэтому add-зануления достаточно. Прямой аналог C-6/D-008 (там ghost «холодных» данных закрыт
активным purge ResourceStore, а не доверием «носитель перезапишет всё»).
Причина: детерминизм (закон №8) и «ничего из воздуха» (закон №3) требуют, чтобы значение поля носителя не
зависело от истории eid. Доверие «система всегда инициализирует все поля» — латентная P0-мина (её и нашли
QA/ревью); единая гарантия на входе надёжнее.
Альтернативы: чистить в `removeComponent`/`destroyEntity` (отклонено — менее полно: не покрывает eid,
который компонент НИКОГДА не носил при reuse через freelist; и дублирует точку чистки. add-зануление —
единственная точка, дающая инвариант «чистый носитель» безусловно; для eid без прошлого массив и так 0);
версионирование eid bitecs (отклонено ранее, D-008 — усложняет кодировку eid/сериализацию). Defensive-добавка той же
правки: `serialize`/`deserialize` вызывают `assertRegistrySorted(registry)` на ИНЪЕЦИРОВАННОМ реестре
(дубль имени молча перезатёр бы колонку — закон №8).
Затрагивает: core/ecs.ts (addComponent — зануление полей), core/snapshot.ts (валидация инъецированного
реестра), core/registry.test.ts + core/snapshot.components.test.ts (гейт-тесты reuse-eid ghost + cross-process
на СВЕЖЕМ ref; проверено — без зануления тесты РЕАЛЬНО краснеют). Голден 481914ae и sim:100days c5dff40f
не изменились (прод-реестр пуст, зануление не затрагивает пустой снапшот).

## D-025 | Фаза 1 / задача 1.1 | 2026-07-03
Решение (ограничения карты, зафиксированы регресс-тестами 1.1): карта Зоны СОДЕРЖИТ намеренные
смертельные зоны — это фича мира, НЕ баг данных, в JSON не «чинить». Инварианты для потребителей:
- Саркофаг (loc 9) — тупик degree=1 (вход только через Припять loc 8), danger=1.0, water=false,
  forage=0.05. Обратный путь к воде ~205 тиков через danger 0.75/0.7.
- Кластер {Рыжий лес 7, Припять 8, Саркофаг 9} — без смежной воды («водная пустыня»); survivable
  (бюджет жажды ~1214 тиков ≫ 205), но узко.
- Кордон (loc 0, ENTRY_LOCATION) — degree=1 (единственный сосед Свалка): бутылочное горлышко трафика.
Обязательства систем: worldgen (1.3) спавнит сталкеров в Кордоне (стартово безопасно), стада — в wild/ruins,
НЕ в Саркофаге. TaskSelection/Movement (1.8/1.4) обязаны взвешивать danger и НЕ гнать NPC в тупик danger=1
без причины (охота/бегство). Вода/еда достижимы из каждой локации (проверено QA) — водяной изоляции нет.
Причина: реализм Зоны требует смертельных зон; но без явного контракта поведение загнало бы NPC на смерть
(нарушение духа DoD «смерти объяснимы» — смерть в тупике без причины необъяснима).
Альтернативы: сделать граф безопасно-равномерным (отклонено — стерильный неатмосферный мир);
запретить вход в danger=1 (отклонено — Саркофаг должен быть достижим для сюжетов поздних фаз).
Затрагивает: worldgen (1.3), TaskSelection (1.8), Movement (1.4), pathfinding; data/map.json (не менять).

## D-026 | Фаза 1 / задача 1.4 | 2026-07-03
Решение (система Movement + детерминированный pathfinding):
1) ЦЕЛЬ движения = `Task.targetLoc` носителя, если у него есть компонент `Task`; иначе/при
   `targetLoc === loc` сущность стоит (`dest === loc`, D-019). Movement НЕ хранит таксономию
   «каким видам задач нужна локация» — для задач без перемещения TaskSelection (1.8) сама
   выставляет `targetLoc = loc`. Так Movement свободен от магии видов задач (закон №7) и не
   пишет `Task` (только читает — чужая зона).
2) Модель транзита без sentinel (D-019): на тике сущность в РОВНО одной ветке — departure
   (`dest = firstStep`, `etaTicks = edgeLen`, publish `move/departed`, БЕЗ списания eta в этот
   тик) ИЛИ decrement/arrival (`etaTicks -= 1`; при `≤0` → `loc = dest`, publish `move/arrived`).
   Одиночный переход = РОВНО `edgeLen` тиков (departure@T → arrival@T+edgeLen). Мультихоп
   эмерджентен: прибыв (`loc = dest`), на СЛЕДУЮЩЕМ тике сущность снова «стоит» и делает
   departure к следующему шагу → между хопами один тик-передышка на узле (контракт B.1).
3) Скорость: `etaTicks = max(MIN_TRAVEL_TICKS, edgeLen)` (balance/movement, закон №7; edgeLen ≥ 1
   ⇒ фактически = edgeLen). Множители ночь/усталость СОЗНАТЕЛЬНО не применяются в 1.4 — связали бы
   Movement с WorldClock/Needs и сломали «ровно edgeLen тиков»; подключит тюнинг позже.
4) Pathfinding — Дейкстра O(V²) на плотных массивах (10 локаций), в отдельном чистом модуле
   `systems/pathfinding.ts` (абстракция `WeightedGraph`, адаптер `MAP_GRAPH` над data). Tie-break
   (закон №8): выбор неулаженного узла обходом по возрастанию id + строгое `<` ⇒ при равной
   дистанции меньший id; релаксация строгим `<` ⇒ предшественник = первое достигшее минимума
   ребро (при равных путях побеждает маршрут через раньше улаженный = меньший id узел). Кратчайший
   путь по `edgeLen` сам не заходит в тупик Саркофаг (degree=1 не лежит на транзитном маршруте),
   поэтому взвешивание danger — забота TaskSelection, не Movement (D-025).
5) Причинность (закон №6, ЗАМКНУТЫЕ цепочки — правка ревью MAJOR-1/MAJOR-2):
   `move/arrived.causedBy` → последний `move/departed` этого eid в логе (departure текущего шага).
   `move/departed.causedBy` различает хопы ноги:
   • ПЕРВЫЙ хоп → самый свежий committed `task/selected` этого eid, У КОТОРОГО
     `payload.targetLoc === текущему Task.targetLoc` (матч по eid И targetLoc — снимает прилипание
     старого события чужой цели; MAJOR-2); нет такого → `null` (не прилепляем чужую цель).
   • ПРОМЕЖУТОЧНЫЙ хоп (departs, только что прибыв в промежуточный узел) → последний `move/arrived`
     этого eid, чтобы цепочка мультихопа не рвалась (MAJOR-1): `task/selected → dep1 → arr1 → dep2
     → arr2 → …`. Различение: совпадающий `task/selected` НОВЕЕ последнего `arrived` (или прибытий
     не было) ⇒ первый хоп; иначе промежуточный.
   ТАЙМИНГ: контракт TaskSelection 1.8 — `task/selected` закоммичен ДО тика departure; финализируется
   при 1.8. Сейчас Movement матчит по (eid,targetLoc) из committed-лога — робастно и без stale
   независимо от точного тика (замена прежней хрупкой привязки к `at(tick-1)`).
   rng НЕ используется (путь детерминирован графом; закон №2 — случайность только для физиологии).
6) В `@zona/shared/events.ts` добавлены члены union `move/departed {eid,from,to}` и
   `move/arrived {eid,at}`. `task/selected` НЕ добавлен (контракт 1.8 — через sim-architect).
7) Инвариант цели (F-2, закон №4): при `firstStep=undefined` (targetLoc невалиден/недостижим)
   сущность стоит молча БЕЗ событий, Movement НЕ падает. Контракт: TaskSelection (1.8) обязана
   давать только валидный достижимый `targetLoc`; молчаливый вечный простой из-за невалидной цели
   ловится world-инвариантом гейта 1.13 («0 idle > N тиков»), а не крашем здесь.
Причина: минимальная, детерминированная система движения, читающая намерение из ECS и общающаяся
через шину с ПОЛНОЙ (замкнутой) причинностью; pathfinding вынесен и покрыт юнит-тестами независимо от ECS.
Альтернативы: departure и следующий hop в один тик (отклонено — контракт B.1 «на следующем тике»);
привязка причины departed к `at(tick-1)` (отклонено ревью — хрупко: null/stale при внутритиковой
невидимости D-005; заменено матчем по (eid,targetLoc) из committed-лога); падать на невалидной цели
(отклонено — латентный idle ловит гейт 1.13, краш в ядре недопустим). Перф чтения лога (сканы O(n))
НЕ трогаем в 1.4 (Movement пока не в прогоне; дешёвый поиск причины — аддендум архитектора перед 1.8);
хранить last-departed id на сущности вместо скана (отклонено для 1.4 — лишнее сериализуемое состояние).
Затрагивает: packages/sim/src/systems/{movement,pathfinding}.ts (+тесты), packages/shared/src/events.ts,
docs/diagrams/movement-1.4.md. Movement НИГДЕ не зарегистрирован в прогоне (CLI/goldens) — sim:100days
c5dff40f и голдены пустого мира не сдвигаются.

## D-027 | Фаза 1 / задача 1.5 → контракт для 1.3 | 2026-07-03
Решение: worldgen (1.3) ОБЯЗАН создавать сталкеров со стартовыми нуждами СТРОГО НИЖЕ критических порогов
(hunger<HUNGER_CRITICAL, thirst<THIRST_CRITICAL и т.д.). Причина: детекция порога в Needs (1.5) —
`prev<crit && next>=crit` (resume-safe, без флага). Сущность, СТАРТУЮЩАЯ на/выше порога, никогда не
пересекает его вверх → `needs/threshold` не публикуется, а урон истощения идёт с тика 0 → смерть от голода
БЕЗ события-причины = разрыв причинной цепочки летописи (закон №6, бьёт по DoD «смерти объяснимы»).
Сталкеры входят в Зону здоровыми (D-021, приток из-за Периметра), поэтому это естественный контракт, а не
костыль. Альтернатива (Needs эмитит начальный threshold для «рождённых критическими») отклонена — усложняет
физиологию ради состояния, которого не должно возникать. Зафиксировано QA-тестами 1.5 (поведение «немого
критического» закреплено как известный контракт).
Затрагивает: worldgen (1.3, стартовые Needs), гейт 1.13 (инвариант: нет смертей без причинной цепочки).

## D-028 | Фаза 1 / задача 1.6 | 2026-07-03
Решение (система Weather + производный день/ночь):
1) Погода использует seeded PRNG ядра — ЛЕГАЛЬНО под закон №2 как «генерация СРЕДЫ» (категория «генерация
   мира», D-019), а НЕ «X% шанс исхода у сущности»: детерминирована от seed, идёт независимо от наличия NPC
   (закон №1). Явно обосновано в docblock weather.ts.
2) RESUME-БЕЗОПАСНОСТЬ (P0, закон №8) — «длительность» и «когда следующая смена» НЕ хранятся в компоненте и
   тем более не в рантайм-состоянии. Всё выводится из сериализуемого `WorldClock.weatherSince` + `seed`:
   длительность текущей погоды = `world.rng.fork('weather-duration@'+weatherSince).int(MIN,MAX)` (balance/weather).
   Зависит ТОЛЬКО от (seed, weatherSince) ⇒ на любом тике после load восстанавливается тождественно. Длительность
   НАМЕРЕННО не зависит от типа погоды (иначе пересчёт после load требовал бы предыдущий тип, который не хранится).
   Тип новой погоды = `world.rng.fork('weather-type@'+tick)`, равновероятно среди WEATHER_TYPES ИСКЛЮЧАЯ текущий
   (from!=to — смена видима); тип хранится в `WorldClock.weather`, пересчёт после load не нужен. Форкаем `world.rng`
   (не `ctx.rng`): `ctx.rng=fork('Weather@'+tick)` зависит от текущего тика, а нужен форк по метке ПРОШЛОГО
   weatherSince (D-009/D-004). Смена — на ближайшем due-тике (every:10), где `tick-weatherSince>=длительность`;
   т.к. weatherSince кратен 10 и MIN/MAX кратны 10, фактический интервал ∈ [MIN,MAX] и кратен 10.
3) Причинность (закон №6): `weather/changed.causedBy` → id предыдущего `weather/changed` в логе (лог сериализуется
   ⇒ цепочка переживает save/load без разрыва), `null` для первой смены. Добавлен член union
   `weather/changed {from,to}` (from/to — КОДЫ = индексы WEATHER_TYPES, число, чтобы shared не тянул перечень из sim).
4) Singleton WorldClock: 0 носителей → no-op; >1 → THROW (баг worldgen, D-019; «обработать первого» замаскировало бы
   баг и привязало бы погоду к порядку eid — консистентно с fail-fast ядра D-024/D-016).
5) День/ночь НЕ хранится (D-019): `isNight(tick)`/`timeOfDay(tick)` — чистые функции в systems/daynight.ts,
   день = `[DAWN_TICK, DUSK_TICK)` от `tick % TICKS_PER_DAY` (balance/weather+time). Импортируется TaskSelection 1.8.
Причина: минимальная детерминированная среда с полной resume-безопасностью без нового сериализуемого/рантайм-состояния.
Альтернативы: хранить длительность в компоненте (отклонено — лишнее поле формата снапшота, когда её и так можно
вывести из weatherSince+seed); хранить «когда следующая смена» в рантайме (отклонено — несериализуемая мина, как
несохранённый флаг, риск дубля/расхождения после load); выводить длительность из ctx.rng (отклонено — зависит от
текущего тика, ломает пересчёт прошлой длительности после resume); строковый payload погоды в shared (отклонено —
связал бы @zona/shared с перечнем из @zona/sim, дублировал бы контент).
Затрагивает: packages/sim/src/systems/{weather,daynight}.ts (+weather.test.ts), packages/shared/src/events.ts
(union weather/changed), docs/diagrams/weather-1.6.md. Weather НИГДЕ не зарегистрирован в прогоне (CLI Фазы 0 —
пустые тики), поэтому голдены 481914ae и sim:100days c5dff40f не сдвигаются.

## D-029 | Фаза 1 / задача 1.7 | 2026-07-03
Решение (контракты/ограничения Perception, зафиксированы тестами):
(1) КОНТРАКТ КОНСЮМЕРОВ contacts: `contacts` в ResourceStore могут ≤1 тик держать eid только что
погибшей сущности (Perception чистит их на следующем прогоне, every:1). Потребители contacts
(TaskSelection 1.8, Encounter 1.10) ОБЯЗАНЫ валидировать `existsEntity(eid)` перед адресацией, иначе
сошлются на покойника. Порядок планировщика (Perception перед TaskSelection/Encounter, Death последний)
де-факто сужает окно, но контракт обязателен для устойчивости.
(2) ОГРАНИЧЕНИЕ: детекция «нового контакта» и spotted идёт по eid, не по идентичности. Если eid покойника
переиспользован новой co-located сущностью в межтиковом окне (Encounter убил → Reproduction родил на том же
eid в той же локации), встреча НОВОГО носителя не публикуется (prev-контакты держат старый eid). Редкий
эмерджентный случай; для Фазы 1 принят как ограничение (потеря одной записи spotted в летописи). Возможный
фикс в будущем — детекция по стабильной идентичности (не по eid), связано с отсутствием версионирования eid
(D-008). resume-детерминизм при этом НЕ страдает (обе ветве прогона ведут себя одинаково).
Затрагивает: systems/perception, потребители contacts (1.8, 1.10), гейт 1.13 (не считать потерю spotted
дефектом причинности смертей — это про встречи, не про смерти).

## D-030 | Фаза 1 | 2026-07-03
Решение: КОНВЕНЦИЯ причинности «id причины в поле состояния». Система, устанавливающая персистентное
состояние-намерение, штампует возвращённый publish() EventId в ui32-поле компонента/записи
(Task.causeEvent, Position.moveCause, Health.lethalCause, contacts.spottedEvent; 0 = нет причины).
Нисходящие системы берут causedBy из поля за O(1), без скана лога.
Причина: шина D-005 отдаёт событие в лог только на endTick(T), at(T-1) не видит причину, выставленную
РАНЬШЕ в том же тике другой системой (порядок B.1). Штамп решает внутритиковую невидимость (publish
возвращает id сразу) и перф (устраняет O(лог) filter на носителя за тик, бюджет 1.6мс D-006). Resume-safe:
поле сериализуется задачей 1.0.
Альтернативы: скан committed-лога на матч (отклонено — O(лог)/тик + хрупкий тайминг при 1.8); мгновенная
бус-доставка в тот же тик (отклонено — D-005); вытянуть P-1 индекс сейчас (отклонено — не нужен, D-032).
Затрагивает: core/registry.ts (поля causeEvent), core/ecs.ts (stampCause+guard), системы 1.4/1.7/1.8/1.10/1.11.

## D-031 | Фаза 1 | 2026-07-03
Решение: EventId в causeEvent-полях как ui32; EventId ≥ 1 (publish: ++eventSeq), 0 = «нет причины».
stampCause бросает при id > 0xFFFFFFFF (fail-fast).
Причина: Uint32Array точен до 2^32-1 ≈ 4.29e9; 100-дневный прогон ~14M–144M событий (запас ×2 порядка).
Шина хранит eventSeq как f64 (точен до 2^53) — усечение возможно ТОЛЬКО при записи в ui32-поле, поэтому
граница проверяется явно. При многолетних прогонах — расширить кодирование (hi/lo ui32).
Затрагивает: core/ecs.ts (stampCause guard), реестр полей.

## D-032 | Фаза 1 | 2026-07-03
Решение: Инвариант порядка — производитель штампа РАНЬШЕ потребителя в фиксированном порядке тика
(Needs<Death, TaskSelection<Movement, Perception<Encounters, Encounters<Death). Иначе потребитель прочтёт
старое значение. Покрыто тестом порядка. TaskSelection перештамповывает Task.causeEvent и публикует
task/selected ТОЛЬКО при СМЕНЕ задачи (не каждый тик) — бортует объём событий, стабилизирует causedBy.
Индекс P-1 остаётся ОТЛОЖЕННЫМ: для per-tick causedBy штамп самодостаточен; P-1 — историческим запросам
(летопись/слухи, Фаза 3), вытянуть при первом потребителе.
Затрагивает: core/scheduler (порядок как инвариант+тест), системы 1.8/1.4/1.7, ROADMAP (P-1).

## D-033 | Фаза 1 / задача 1.8 | 2026-07-03
Решение (TaskSelection — utility-AI + ретрофит Movement на штампы):
1) ОЦЕНКИ из СОСТОЯНИЯ (закон №2, НЕ «X% шанс»). Нужды нормируются /NEED_MAX ∈[0..1];
   safety(loc)=1−danger. Формулы (веса ТОЛЬКО из balance/utility.ts, закон №7):
   SLEEP=W.fatigue·fat+(night?W.night:0)+safety·W.safe; EAT=W.hunger·hun+W.food (лишь при
   наличии еды в инвентаре); DRINK=W.thirst·thi+waterHere·W.water; HUNT=W.hunger·hun+
   gameAbund·W.game+survival·W.skill−fear·W.fear−(night?W.nightHunt:0) (лишь при достижимой
   живой дичи); FLEE=W.fleeFear·fear; FORAGE=FALLBACK_SCORE_FLOOR+W.forageBase·forage; REST=
   W.restBase+W.fatigue·fat·REST_FATIGUE_FACTOR. Два fallback (FORAGE/REST) строго >0 ⇒ argmax
   никогда не пуст, idle невозможен (закон №4, D-020).
2) EAT БЕЗ еды и HUNT БЕЗ достижимой дичи ИСКЛЮЧАЮТСЯ из argmax (−∞), а не выбираются «в
   пустоту»: нельзя есть отсутствующее (закон №3), нельзя охотиться, где дичи нет; иначе NPC
   залип бы на no-op-задаче (латентный idle). Голод без еды → HUNT (если дичь)/FORAGE.
3) argmax ДЕТЕРМИНИРОВАН (D-020, закон №8): кандидаты в порядке ВОЗРАСТАНИЯ кода TaskKind со
   строгим `>` ⇒ на ТОЧНОМ равенстве побеждает МЕНЬШИЙ код (SLEEP<EAT<DRINK<FORAGE<HUNT<REST<
   FLEE), НЕ rng-tie-break. rng в решении не используется. Тай-брейк закреплён тестом (loc9
   Саркофаг: SLEEP==REST при fatigue=25 — точный double-тай — берётся SLEEP).
4) НОВЫЕ веса добавлены в balance/utility.ts (закон №7): W.fleeFear=1.5, W.nightHunt=0.5,
   W.restBase=0.1, REST_FATIGUE_FACTOR=0.5. Формулы контракта на них ссылались; хранить их вне
   balance было бы магическим числом.
5) УГРОЗА влияет на выбор ЧЕРЕЗ Needs.fear (его поднимает Perception 1.7 от co-located угрозы),
   а НЕ прямым чтением contacts. Контракт 1.8 предлагал собирать contacts, но в заданных
   формулах они не участвуют (fear уже инкапсулирует «рядом зверь»), а валидировать/держать
   неиспользуемые eid — мёртвый вход и лишняя связанность. FLEE-направление = самый безопасный
   сосед (min danger, tie min id). Так соблюдены закон №6 (общение через компонент Needs.fear) и
   D-029 (не обращаемся к возможно-мёртвым eid из contacts вовсе). Если позже FLEE понадобится
   избегать КОНКРЕТНОГО соседа-источника угрозы — тогда и подключим валидированные contacts.
6) ЦЕЛЬ задачи всегда валидна/достижима (D-026): EAT/FORAGE/REST→loc; SLEEP→Home.loc; DRINK→
   ближайшая loc с водой по edgeLen (текущая, если с водой); HUNT→ближайшая loc с живой дичью
   (min-eid особь); FLEE→безопаснейший сосед. Ближайшее — детерминированный Дейкстра, tie по
   стоимости → меньший id локации.
7) РЕТРОФИТ Movement на штампы (D-030, снят перф-хвост D-026 — скан лога O(лог)/носителя/тик):
   move/departed.causedBy=Task.causeEvent (id task/selected, O(1) из компонента); id departed
   штампуется в Position.moveCause; move/arrived.causedBy=Position.moveCause. РЕКОНСИЛЯЦИЯ: текст
   контракта 1.8 буквально предлагал «скопировать Task.causeEvent→moveCause» (тогда arrived
   ссылался бы на task/selected), НО docblock самого поля Position.moveCause (1.2b) фиксирует, что
   оно хранит EventId move/departed. Выбран вариант ПОЛЯ: moveCause=id departed ⇒ цепочка каждого
   хопа ЗАМКНУТА как chain task/selected→departed→arrived (богаче «звезды», совпадает с целевой
   формулировкой контракта «task/selected→departed→arrived» и сохраняет прежние Movement-тесты
   arr.causedBy===dep.id). Старые лог-сканы (findMatchingTaskSelected/findLastMoveEvent/
   departureCause) удалены. В мультихопе все departed ноги ссылаются на один task/selected (штамп
   стабилен, задача не менялась), каждый arrived — на свой departed.
8) БАЛАНС-ХВОСТ (для balance-analyst): при стартовых весах в безопасном доме (Кордон, safety
   0.95) SLEEP доминирует, пока нет срочной нужды — из-за safety·W.safe + W.fatigue·fatigue.
   Полноценный СУТОЧНЫЙ ЦИКЛ (сон→спад усталости→голод→еда→…) замкнётся ТОЛЬКО с восстановлением
   нужд (задача 1.8e): здесь fatigue лишь растёт, поэтому worldgen-популяция монотонно «спит в
   лагере». Система корректна/детерминирована; конкретные веса и потолок safety-бонуса —
   предмет тюнинга balance-analyst. TaskSelection НЕ включён в CLI-прогон (голдены 481914ae/
   c5dff40f не сдвинуты).
Причина: детерминированный, объяснимый выбор задачи из состояния без idle; причинность per-tick
через O(1)-штампы вместо сканов лога.
Альтернативы: rng-tie-break (отклонено — D-020); чтение contacts в оценках (отклонено — мёртвый
вход при формулах на Needs.fear); moveCause=id task/selected (отклонено — рвёт chain departed→
arrived и ломает прежние тесты, противоречит docblock поля).
Затрагивает: systems/task-selection (+тест), systems/movement (ретрофит+тест), balance/utility
(новые веса), shared/events (union task/selected), core/components (Task/Position — без изменений
схемы). ХВОСТ 1.8e: восстановление нужд замкнёт распорядок.

## D-034 | Фаза 1 / задача 1.8 → контракты для 1.13/balance | 2026-07-03
Решение (хвосты TaskSelection, зафиксированы тестами/диаграммой):
(1) КОНТРАКТ idle (закон №4): TaskSelection запрашивает [Human, Alive, Needs] — живой Human БЕЗ компонента
Needs был бы молча пропущен и остался без Task (idle). worldgen (1.3) всегда навешивает Needs, поэтому
дыры на практике нет, но инвариант «каждая живая Human всегда с Task» держится КОСВЕННО через worldgen.
Гейт 1.13 ОБЯЗАН иметь world-инвариант: нет живого Human без Needs И нет сущности без Task > N тиков (0 idle).
(2) БАЛАНС-ХВОСТ FLEE: safestNeighbor уводит в наименее опасного СОСЕДА даже когда текущая loc безопаснее
всех соседей («бегство в бóльшую опасность»). Не баг (fear поднимает только co-located угроза), но кандидат
на тюнинг balance-analyst (учесть «остаться, если тут безопаснее»; риск осцилляций при 1.8e).
(3) Порядок систем (D-032): инвариант TaskSelection<Movement пока держится регистрацией в тестах; закрепить
ассертом-тестом при сборке прод-конвейера (гейт 1.13).
Затрагивает: гейт 1.13 (world-инварианты + тест порядка), balance-analyst (веса FLEE), systems/task-selection.

## D-035 | Фаза 1 / задача 1.8e | 2026-07-03
Решение (система TaskEffects — исполнение задач восстанавливает нужды + физический расход еды):
1) УСЛОВИЕ ИСПОЛНЕНИЯ: эффект применяется ТОЛЬКО если сущность СТОИТ у цели (`Position.dest === loc`,
   D-019). В транзите задача не исполняется (нельзя есть/пить/спать шагая). Порядок в тике (D-032):
   TaskEffects ПОСЛЕ Movement (сущность уже прибыла/стоит на этом тике) и ДО Encounters. Пара
   «Needs 1.5 растит ↔ TaskEffects закрывает» замыкает суточный цикл эмерджентно, без расписания (закон №1/№2).
2) EAT — ФИЗИЧЕСКИЙ РАСХОД (закон №3): съедается ОДНА единица САМОЙ ПИТАТЕЛЬНОЙ еды (kind='food',
   qty>0) из inventory; tie по nutrition → первая по уже-отсортированному по item инвентарю (детерминизм,
   закон №8). qty−1; при qty→0 запись УДАЛЯЕТСЯ (сортировка сохраняется — прочие в прежнем порядке).
   hunger −= nutrition (items.json), кламп 0. Инвентарь пишется НОВЫМ массивом (не мутация in-place,
   изоляция ссылок). Нет еды → no-op (устойчивость: TaskSelection не выберет EAT без еды, но система
   не полагается на это). Суммарно предметов уменьшается ровно на съеденное — тест «−ровно съеденное».
3) DRINK/FORAGE — из СРЕДЫ, не из воздуха (закон №3): DRINK берёт воду из loc.water (река/колодец),
   FORAGE — подножный корм из loc.forage; ни то, ни другое не создаёт/не тратит предмет инвентаря.
   Восстановление: DRINK thirst −= DRINK_RECOVERY_PER_TICK (если loc.water); FORAGE hunger −=
   FORAGE_RECOVERY_PER_TICK * loc.forage (в бедной локации почти ноль). Кламп 0.
4) SLEEP/REST — усталость: SLEEP эффективен ДОМА (loc===Home.loc; носитель без Home → homeLoc=loc,
   спит на месте, консистентно с TaskSelection D-033) на SLEEP_RECOVERY_PER_TICK; стоя SLEEP ВНЕ дома
   (носитель Home away from home) — БЕЗ эффекта. REST — в ЛЮБОЙ локации на REST_RECOVERY_PER_TICK,
   СОЗНАТЕЛЬНО < SLEEP (привал слабее сна). HUNT/FLEE — БЕЗ восстановления здесь (мясо даёт Encounter
   1.10; FLEE — только движение Movement).
5) СТАВКИ — ТОЛЬКО в balance/needs.ts (закон №7): добавлены REST_RECOVERY_PER_TICK=0.06,
   DRINK_RECOVERY_PER_TICK=20 (полная жажда за ~5 тиков), FORAGE_RECOVERY_PER_TICK=0.5 (× loc.forage);
   SLEEP переиспользует существующую SLEEP_RECOVERY_PER_TICK=0.15 (REST<SLEEP закреплено тестом). EAT
   не имеет «ставки» — восстановление = nutrition предмета (данные, не баланс).
6) БЕЗ СОБЫТИЙ: TaskEffects не публикует событий — расход инвентаря и изменение Needs суть состояние,
   читаемое нисходящими системами через компоненты/ресурсы (закон №6 не требует события без адресата).
   Летопись «поел/попил» — надстройка нарратива (Фаза 3+); добавление члена union + штампа causedBy без
   потребителя было бы мёртвой поверхностью. rng НЕ используется (восстановление арифметическое, закон №2/№8).
7) RESUME (закон №8): Needs (f32-компоненты) и inventory (ResourceStore) сериализуются 1.0 ⇒ непрерывный
   прогон === split через save/load (гейт-тест на полном конвейере Needs+Perception+TaskSelection+Movement+
   TaskEffects). TaskEffects НЕ зарегистрирован в CLI-прогоне ⇒ голдены 481914ae и sim:100days c5dff40f
   не сдвинулись.
Причина: замкнуть распорядок дня (закрытие нужд, хвост D-033) детерминированно и с физическим расходом еды.
Альтернативы: класть собранную FORAGE-еду в инвентарь (отклонено — проще съесть на месте, источник — среда);
публиковать item/consumed (отклонено для 1.8e — нет потребителя, лишняя поверхность union/causedBy);
per-tick discrete DRINK-предмет из инвентаря (отклонено — вода из источника локации, не предмет, закон №3);
съедать первую по сортировке, а не питательнейшую (отклонено — «есть лучшее» осмысленнее, tie всё равно
детерминирован сортировкой). Затрагивает: systems/task-effects.ts (+тест), balance/needs.ts (3 ставки).
ХВОСТ balance-analyst: конкретные ставки восстановления — предмет тюнинга по метрикам 100-дневного прогона.

## D-036 | Фаза 1 / задача 1.8 (пост-фикс переедания, найдено QA/ревью 1.8e) | 2026-07-03
Решение: Формула EAT в TaskSelection ЗАВИСИТ ОТ ГОЛОДА, без плоского слагаемого:
`sEat = hasFood ? (W.hunger + W.food)·hunger : −∞` (было `W.hunger·hunger + W.food`).
Причина: плоский бонус W.food(0.3) делал EAT победителем argmax даже при hunger≈0 → сытый
сталкер с едой выбирал EAT, а TaskEffects (1.8e) исполнял его ВСЛЕПУЮ и палил весь пищевой
запас по единице/тик без пользы (в сухой локации — весь стек; в водном поселении DRINK частично
перехватывал). Это иррационально («ест сытым») и подрывало пищевую экономику (спирит закона №3 —
необоснованная ПОТЕРЯ ресурса), искажало бы метрики гейта 1.13. Теперь: при hunger≈0 EAT→~0 и
проигрывает fallback (FORAGE/REST/SLEEP) — сытый не ест; при реальном голоде бонус W.food (тоже
масштабированный голодом) поднимает EAT НАД HUNT в типичных условиях — рационально доесть запас,
прежде чем идти на риск охоты. Константы прежние (W.hunger/W.food в balance/utility, закон №7),
доп. порогов не вводили — выбран чистый вариант «оба слагаемых ×hunger».
Проверка (закон №8): детерминизм/resume целы; голдены 481914ae/c5dff40f не сдвинуты (TaskSelection
не в CLI-прогоне). Тесты: task-selection.test.ts — сытый с едой НЕ выбирает EAT (новый регресс),
голодный с едой (даже при дичи рядом) выбирает EAT, EAT==DRINK тай перекалиброван на новую формулу
(hunger=66,thirst=36, бит-точный тай, побеждает EAT). task-effects.test.ts — характеризующие тесты
переедания ИНВЕРТИРОВАны в регресс (запас сытого не горит); выживательный сценарий сохранил вывод
(конечный запас → истощение → рибаунд голода к критическому → падение hp → мотив HUNT), но на
РЕАЛИСТИЧНОМ горизонте: истощение не мгновенное (~1200+ тиков), а не <10 (артефакт переедания снят).
Механика TaskEffects «EAT при hunger=0 списывает единицу» НЕ тронута — фикс в ВЫБОРЕ, не в исполнении.
Затрагивает: systems/task-selection (формула EAT), balance/utility (док W.food), тесты
task-selection/task-effects. Дополняет D-033.
